<%= form_for [list, list.items.new] do |f| %>
    <div class="form-group">
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter another To-do item" %>
    </div>
    <div class="form-group">
      <%= f.submit "Add To-do", class: 'btn btn-success' %>
    </div>
<% end %>